#include <stdio.h>

/**
 * main - the program's main function
 *
 * Return: return 0 (successful)
 */

int main(void)
{
	int count;

	for (count = 48; count < 58; count++)
	{
		putchar(count);
	}
	putchar('\n');
	return (0);
}
