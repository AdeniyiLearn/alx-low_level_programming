#include <stdio.h>

/**
 * main - the program's main function
 *
 * Return: return 0 (successful)
 */

int main(void)
{
	int count;

	for (count = 0; count < 10; count++)
	{
		printf("%d", count);
	}
	putchar('\n');
	return (0);
}
