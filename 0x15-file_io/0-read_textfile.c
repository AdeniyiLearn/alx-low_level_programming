#include "main.h"

/**
 * read_textfile - function that converts a binary number to an unsigned int.
 * @filename: const parameter of type char
 * @letters: second parameter
 * Return: converted number or 0 if there is one or more chars in the string b
 * that is not 0 or 1 b or is NULL failed
 *
 */

ssize_t read_textfile(const char *filename, size_t letters)
{

	return (score);
}
