#include <stdio.h>
#include "main.h"
/**
 * add - function adds the two inputs together
 * @a: first input
 * @b: second input
 * Return: returns result
 */

int add(int a, int b)
{
	return (a + b);
}
